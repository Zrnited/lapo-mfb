export type NavLinks = {
  title: string;
  icon: string;
  iconActive?: string;
  to: string;
};

export type RequestCardRequests = {
  branch: string;
  cardType: string;
  quantity: number;
  status: string;
};
